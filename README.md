pra executar o client:

- python run.py

pra executar o server:

- python run.py

utils/cryypto_service:

- Agregador de funcionalidades/estratégias de geração de cvhaves (Diff-Hellman) e criptografia (XOR + HMAC)

client/client_manager:

- Contém a lógica de Regra de Negócio, o estado do cliente (chaves) e a delegação das operações criptográficas.

client/chat_client:

- Classe principal de I/O de rede (Handler).

---

backend/server_manager:

- controller de grupos

backend/client_handler:

- Classe principal de I/O de rede
